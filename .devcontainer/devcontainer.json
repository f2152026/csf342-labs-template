{
  "name": "CS F342 CA Labs",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "mshr-h.veriloghdl",
        "lramseyer.vaporview"
      ]
    }
  },
  "remoteUser": "vscode",
  "postCreateCommand": "chmod +x scripts/*.sh && (code --install-extension classroom50-helper-extension/*.vsix || code-insiders --install-extension classroom50-helper-extension/*.vsix || true)"
}
